
#include <math.h>
#include "../FADEC_Constants_H/ConstantsTJ300R.h"
#include "../../FADEC_Defines_H/Control_Types.h"

void getTJ300RFADECContConst(struct ControllerConstants *ContConst)
{

    /* Loads controller parameters */

    //ContConst->TimeIgnition = TJ300R_TIME_IGNITION;
    ContConst->Alt_Elem = TJ300R_ALT_ELEM;
    ContConst->As_Elem = TJ300R_AS_ELEM;
    ContConst->Ath = TJ300R_Ath_NOZZLE;
    ContConst->BoostTime = TJ300R_BOOST_TIME;
    ContConst->CLLever = TJ300R_CLOOP;
    ContConst->EmergencyCom = TJ300R_COMMAND_EMERGENCY;
    ContConst->EmergencyWait = TJ300R_WAIT_EMERGENCY_COMMAND;
    ContConst->ErrorFracSD = TJ300R_SD_ERRORFRAC;
    ContConst->FNIdle = TJ300R_FN_IDLE;
    ContConst->Fn_Elem = TJ300R_FN_ELEM;
    ContConst->FuelPumpTimeConstant = TJ300R_FUELPUMPTIMECONSTANT;
    ContConst->FuelRampSD = TJ300R_SD_FUELRAMP;
    ContConst->KbBear = TJ300R_BEAR_KB;
    ContConst->KbN = TJ300R_KBN;
    ContConst->KbT = TJ300R_KBT;
    ContConst->KiBear = TJ300R_BEAR_KI;
    ContConst->KiTt4 = TJ300R_KITT4;
    ContConst->KpBear = TJ300R_BEAR_KP;
    ContConst->KpTt4 = TJ300R_KPTT4;
    ContConst->MN_Elem = TJ300R_MN_ELEM;
    ContConst->ManualMode = TJ300R_MANUAL;
    ContConst->MaxInterFault = TJ300R_MAXINTERFAULT;
    ContConst->NDes = TJ300R_NDES;
    ContConst->NMax = TJ300R_NMAX;
    ContConst->NMaxRef = TJ300R_NMAX;
    ContConst->NcHPCMAP_Elem = TJ300R_FAN_NC;
    ContConst->NumBear = TJ300R_BEAR_NUM;
    ContConst->NumPFuelSens = TJ300R_PFUELSENS;
    ContConst->NumPOilSens = TJ300R_POILSENS;
    ContConst->NumPs3Sens = TJ300R_PS3SENS;
    ContConst->NumPs3SensTrust = TJ300R_PS3SENSTRUST;
    ContConst->NumRPMSens = TJ300R_RPMSENS;
    ContConst->NumRPMSensTrust = TJ300R_RPMSENSTRUST;
    ContConst->NumSteps = TJ300R_NUMSTEPS;
    ContConst->NumTs2Sens = TJ300R_TS2SENS;
    ContConst->NumTs2SensTrust = TJ300R_TS2SENSTRUST;
    ContConst->NumTt4Sens = TJ300R_TT4SENS;
    ContConst->NumTt4SensTrust = TJ300R_TT4SENSTRUST;
    ContConst->NumVibrationSens = TJ300R_VIBRATIONSENS;
    ContConst->OPRPMInt = TJ300R_BEAR_OPRPMINT;
    ContConst->OPRPMMax = TJ300R_BEAR_OPRPMMAX;
    ContConst->OPRPMMin = TJ300R_BEAR_OPRPMMIN;
    ContConst->PFUelModel = TJ300R_PFUELMODEL;
    ContConst->PFuelHigh = TJ300R_PFUELHIGH;
    ContConst->PFuelLow = TJ300R_PFUELLOW;
    ContConst->PFuelOpen = TJ300R_PFUELOPEN;
    ContConst->PFuelShort = TJ300R_PFUELSHORT;
    ContConst->PFuelThreshold = TJ300R_PFUELTHRESHOLD;
    ContConst->POilAlarm = TJ300R_POIL_ALARM;
    ContConst->POilHigh = TJ300R_POILHIGH;
    ContConst->POilLow = TJ300R_POILLOW;
    ContConst->POilModel = TJ300R_POILMODEL;
    ContConst->POilOpen = TJ300R_POILOPEN;
    ContConst->POilShort = TJ300R_POILSHORT;
    ContConst->POilShutDown = TJ300R_POIL_SHUTDOWN;
    ContConst->POilThreshold = TJ300R_POILTHRESHOLD;
    ContConst->PSOpen = TJ300R_PSSOPEN;
    ContConst->PSOpenThresh = TJ300R_PSOPENTHRESH;
    ContConst->PSShort = TJ300R_PSSHORT;
    ContConst->PSShortThresh = TJ300R_PSSHORTTHRESH;
    ContConst->PSThreshold = TJ300R_PSTHRESHOLD;
    ContConst->PSensTimeConst = TJ300R_PSENSTIMECONST;
    ContConst->Pt3BlowOut = TJ300R_Pt3BOUT;
    ContConst->Pt3ShaftLock = TJ300R_Pt3SHLCK;
    ContConst->Pt3Threshold = TJ300R_PT3THRESHOLD;
    ContConst->RPMBlowOut = TJ300R_RPM_BOUT;
    ContConst->RPMCLStartMaxRef = TJ300R_RPM_CLSTARTMAXREF;
    ContConst->RPMCLStartRamp = TJ300R_RPM_CLSTARTRAMP;
    ContConst->RPMCLStartRampFaster = TJ300R_RPM_CLSTARTRAMPFASTER;
    ContConst->RPMIdle = TJ300R_RPM_IDLE;
    ContConst->RPMIgnOff = TJ300R_RPM_IGN_OFF;
    ContConst->RPMMinSD = TJ300R_SD_RMPMIN;
    ContConst->RPMOilInt = TJ300R_BEAR_RPMOILINT;
    ContConst->RPMOilOn = TJ300R_BEAR_RPMOILON;
    ContConst->RPMOpen = TJ300R_RPMOPEN;
    ContConst->RPMRampChange = TJ300R_RPM_RAMP_CHANGE;
    ContConst->RPMRelaysOn = TJ300R_RPM_RELAYS_ON;
    ContConst->RPMShort = TJ300R_RPMSHORT;
    ContConst->RPMThreshold = TJ300R_RPMTHRESHOLD;
    ContConst->RPMValve100 = TJ300R_RPM_VALVE_100;
    ContConst->RPMWindmill = TJ300R_RPM_WINDMILL;
    ContConst->RUMin = TJ300R_RUMIN;
    ContConst->RefRampValue = TJ300R_REFRAMP;
    ContConst->ShutDownCom = TJ300R_COMMAND_SHUTDOWN;
    ContConst->ShutDownWait = TJ300R_WAIT_SHUTDOWN_COMMAND;
    ContConst->StartCom = TJ300R_COMMAND_START;
    ContConst->StartWait = TJ300R_WAIT_START_COMMAND;
    ContConst->StrtInit = TJ300R_STRTINIT;
    ContConst->StrtMax = TJ300R_STRTMAX;
    ContConst->StrtRPMOff = TJ300R_STRTRPMOFF;
    ContConst->StrtRampCoeff = TJ300R_STRTRAMPCOEFF;
    ContConst->StrtWhich = TJ300R_STRTWHICH;
    ContConst->TCOpen = TJ300R_TCOPEN;
    ContConst->TCThreshold = TJ300R_TCTHRESHOLD;
    ContConst->TMaxBear[0] = TJ300R_BEAR_TMAX_0;
    ContConst->TMaxBear[1] = TJ300R_BEAR_TMAX_1;
    ContConst->TMaxBear[2] = TJ300R_BEAR_TMAX_2;
    ContConst->TRefBear[0] = TJ300R_BEAR_TREF_0;
    ContConst->TRefBear[1] = TJ300R_BEAR_TREF_1;
    ContConst->TRefBear[2] = TJ300R_BEAR_TREF_2;
    ContConst->TTCTimeConst = TJ300R_TTCTIMECONST;
    ContConst->TimeAccomodation = TJ300R_TIME_ACCOMODATION;
    ContConst->TimeBearOverTemp[0] = TJ300R_TIME_BEAR0OT;
    ContConst->TimeBearOverTemp[1] = TJ300R_TIME_BEAR1OT;
    ContConst->TimeBearOverTemp[2] = TJ300R_TIME_BEAR2OT;
    ContConst->TimeBeforeRamp = TJ300R_TIME_BEFORE_RAMP;
    ContConst->TimeFaultAnaylsis = TJ300R_TIME_FAULTANALYSIS;
    ContConst->TimeHighPFuel = TJ300R_TIME_PFUELHIGH;
    ContConst->TimeHighPOil = TJ300R_TIME_POILHIHG;
    ContConst->TimeHighVibration = TJ300R_TIME_VIBHIGH;
    ContConst->TimeIgnition = TJ300R_TIME_IGNITION;
    ContConst->TimeLowPFuel = TJ300R_TIME_PFUELLOW;
    ContConst->TimeLowPOil = TJ300R_TIME_LOW_POIL;
    ContConst->TimeLowPOil = TJ300R_TIME_POILLOW;
    ContConst->TimeOverspeed = TJ300R_TIME_OVERSPEED;
    ContConst->TimeOvertemp = TJ300R_TIME_OVERTEMP;
    ContConst->TimeOvertemp = TJ300R_TIME_OVERTEMP;
    ContConst->TimeReadytoStart = TJ300R_TIME_READYTOSTART;
    ContConst->TimeToEmergSD = TJ300R_SD_TIME_TOEMERG;
    ContConst->TimeToRampSD = TJ300R_SD_TIME_TORAMP;
    ContConst->TimeWindmill = TJ300R_TIME_WINDMILL;
    ContConst->Ts2Threshold = TJ300R_TS2THRESHOLD;
    ContConst->Tt4AvFlameOn = TJ300R_Tt4AV_FLAME_ON;
    ContConst->Tt4AvMax = TJ300R_Tt4AV_MAX;
    ContConst->Tt4BlowOut = TJ300R_Tt4BOUT;
    ContConst->Tt4CLRampCoef = TJ300R_Tt4_CLRAMPCOEF;
    ContConst->Tt4CLSafeStart = TJ300R_Tt4_CLSAFESTART;
    ContConst->Tt4Max = TJ300R_TT4MAX;
    ContConst->Tt4MaxRef = TJ300R_TT4MAXREF;
    ContConst->Tt4RampDecFast = TJ300R_Tt4_RAMPDECFAST;
    ContConst->Tt4ShaftLock = TJ300R_Tt4SHLCK;
    ContConst->Tt4Threshold = TJ300R_TT4THRESHOLD;
    ContConst->UseEngUnits = TJ300R_ENGUNITS;
    ContConst->ValveInit = TJ300R_VALVE_INIT;
    ContConst->ValveTotal = TJ300R_VALVE_TOTAL;
    ContConst->VibHigh = TJ300R_VIBHIGH;
    ContConst->VibModel = TJ300R_VIBMODEL;
    ContConst->VibOpen = TJ300R_VIBOPEN;
    ContConst->VibOpenThresh = TJ300R_VIBOPENTHRESH;
    ContConst->VibShort = TJ300R_VIBSHORT;
    ContConst->VibShortThresh = TJ300R_VIBSHORTTHRESH;
    ContConst->VibThreshold = TJ300R_VIBTHRESHOLD;
    ContConst->WaitAction = TJ300R_WAIT_ACTION;
    ContConst->WcHPCMAP_Elem = TJ300R_FAN_NC * TJ300R_FAN_PR;
    ContConst->Wc_per_Nc_HPCMAP = TJ300R_FAN_PR;
    ContConst->WfHighPulse = TJ300R_FUEL_HIGH_PULSE;
    ContConst->WfImpulseTime = TJ300R_FUEL_IMPULSE_TIME;
    ContConst->WfInit = TJ300R_FUEL_INIT;
    ContConst->WfKdStart = TJ300R_FUEL_KDSTART;
    ContConst->WfMax = TJ300R_WFMAX;
    ContConst->WfPerc = TJ300R_FUEL_PERC;
    ContConst->WfRamp = TJ300R_FUEL_RAMP;
    ContConst->WfRampDec = TJ300R_FUEL_RAMP_DEC;
    ContConst->WfRampDecFast = TJ300R_FUEL_RAMP_DEC_FAST;
    ContConst->WfRampDecLowLim = TJ300R_FUEL_RAMP_DEC_LOWLIM;
    ContConst->WfRampLow = TJ300R_FUEL_RAMP_LOW;

}